import { assert, expect, test, getInbox, launch, dotenv, PNG, dateFns, faker, axios, fse, playwright } from '../../qawHelpers';

test("hgse_3847_mobile_view_add_spinner_while_loading_search_results_on_map", async () => {
 // Step 1. HGSE-3847: [Mobile View] Add spinner while loading search results on map
  //--------------------------------
  // Arrange:
  //--------------------------------
  
  // 1
  // Login to https://qa-portal.homegeniusrealestate.com/ or https://uat-portal.homegeniusrealestate.com/
  // Application is launched
  // 2
  // Navigate and make a search
  // List of properties are displayed
  // 3
  // Select Map option and make a search
  // >While loading results on map replace the listings count with a spinner until the results are finally loaded, then show the results number
  // While loading results on map replaced with a listing count with a spinner until the results are finally loaded, then showed the results number
  
   
  
  //--------------------------------
  // Act:
  //--------------------------------
  
  
  
  //--------------------------------
  // Assert:
  //--------------------------------
  
  
  
});